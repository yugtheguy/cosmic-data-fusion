[pytest]
# Pytest configuration for COSMIC Data Fusion

# Filter warnings
filterwarnings =
    # Ignore joblib warning about physical cores (Windows-specific issue)
    ignore::UserWarning:joblib.externals.loky.backend.context
    # Future pandas warnings are informational only
    ignore::FutureWarning:pandas
